
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cross.trt',
  appName: 'sacncross',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
